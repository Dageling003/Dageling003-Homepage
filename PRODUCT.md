# PRODUCT.md

> 这份文档回答的不是「怎么做」而是「为什么做，做给谁」。
> 想看技术架构走 [docs/architecture.md](./docs/architecture.md)；想看未来路线走 [ROADMAP.md](./ROADMAP.md)；
> 想贡献代码但不确定优先级——**从这里开始**。

**最后更新**：2026-07-25 · 当前版本 v1.0.0

---

## 1. 一句话定位

> **让一个人拥有一个可以长期维护的、无需改 JSON 的个人主页。**

从 [Simple-Homepage](https://github.com/QNquenan/Simple-Homepage) 起步，保留极简美学，
但把"改主页"这件事从「翻代码 → 改 JSON → 重新部署」压缩到「打开 /admin → 点几下 → 立即生效」。

---

## 2. 北极星指标 (North Star Metric)

> **首次部署到成功打开自定义主页 ≤ 15 分钟**（对一个从未用过 Docker 的技术爱好者）

- 不追求 DAU，因为个人主页天生流量小
- 不追求功能覆盖率，因为堆功能会稀释美学
- **只追求一件事**：让"想要一个体面主页"这个念头，尽可能少地被工程细节劝退

派生指标：
- 部署脚本 `deploy.sh` 走完不出错的比例（目标 > 95%）
- `/admin/setup` 首次进入到关闭引导页的中位时间（目标 ≤ 3 分钟）
- 从截图到「我要不要装一个」的转化：先看 README → 再看 demo → 再点 fork

---

## 3. 目标用户 (Personas)

### P1 · 大学生 / 应届开发者（**主目标**，占比 70%）
- **场景**：想要一个能挂 GitHub Pages 之外域名的个人主页，用来放简历、社交链接、学习进度
- **痛点**：不会/不想写前端组件、不会调 CI/CD、看到 `package.json` 就头大
- **能接受**：买个便宜 VPS、跟着 5 步 README 敲命令
- **不能接受**：改一次昵称就要重新 build 一次镜像

### P2 · 前端 / 独立开发者（**次目标**，占比 25%）
- **场景**：已经会写前端，但不想为"就一个人用的主页"重复造轮子
- **痛点**：现有主页项目要么是纯静态（改起来麻烦）要么是 CMS 级的重装备
- **能接受**：自己二次开发主题、扩展一个组件
- **不能接受**：项目死代码多、抽象层套抽象层、想改一处得读半小时

### P3 · 想要多语言 / 全球化个人品牌的创作者（**边缘**，占比 5%）
- 只在 P1 / P2 稳定后再看
- 需要 i18n、RSS、访客统计

**非目标用户**（明确不服务）：
- ❌ 需要多用户 / 团队协作 → 用 CMS
- ❌ 需要博客文章管理（>100 篇）→ 用 Ghost / Astro
- ❌ 需要动态内容（评论、论坛、商城）→ 用 WordPress
- ❌ 需要完全 no-code、连 Docker 都不想碰 → 用 Notion / Bento.me

---

## 4. 核心场景 (Core Journeys)

按重要性排序，每一条都对应一个"必须跑通"的路径：

### Journey A · 首次部署（P1，最高优先级）

```
拿到域名 / VPS
    ↓
git clone → 跑 deploy.sh
    ↓
按提示填 3 个变量（域名 / JWT_SECRET / ACME_EMAIL）
    ↓
打开 https://<域名>/admin/setup
    ↓
设置管理员密码 → 进入后台
    ↓
改姓名、头像、社交链接
    ↓
访问 https://<域名>/ 看到自己的主页
```

**成功指标**：全程不需要读源码、不需要 debug、不需要谷歌任何报错。

### Journey B · 日常改主页内容（P1，最高优先级）

```
打开 /admin
    ↓
改 typewriter 文案 / 加一个技术栈 / 换头像
    ↓
无需重启、无需 build，前台立即生效
```

### Journey C · 二次开发一个新组件（P2，次优先级）

```
读 docs/dev-guide.md
    ↓
在 apps/frontend/src/components/ 里加一个 .vue
    ↓
挂到 HomeView.vue
    ↓
（可选）加一个后台可配置字段：configFields.ts → 立即在 /admin 出现表单
```

**成功指标**：从"我想加一个 XX 卡片"到"上线"不超过 1 小时。

### Journey D · 迁移到新服务器（P1/P2，中优先级）

```
scripts/backup.sh 打包
    ↓
在新机器 rsync 过去
    ↓
docker compose up
    ↓
数据完整可用
```

---

## 5. 功能优先级 (Priority Matrix)

用 **RICE** 简化版（Reach × Impact / Effort，1-5 分）打分：

| 功能 | 用户群 | Impact | Effort | 分值 | 状态 |
|------|--------|--------|--------|------|------|
| 一条命令部署 | P1/P2 | 5 | 3 | **高** | ✅ v1.1 |
| 可视化后台覆盖所有前台字段 | P1 | 5 | 4 | **高** | 🚧 部分 |
| 头像上传 + 图床（TOS/S3） | P1 | 4 | 3 | 中 | 📋 |
| 主题预设（≥5 套） | P1 | 4 | 2 | **高** | 📋 |
| 前端 self-host 字体 / 图标（国内可用） | P1 | 5 | 1 | **高** | ✅ 2026-07 |
| 后端 e2e 测试（登录 / 密码重置 / 配置 CRUD） | P2 | 4 | 3 | 中 | 📋 |
| 前端 E2E（关键路径） | P2 | 3 | 4 | 中 | 📋 |
| PWA / 离线可用 | P1 | 2 | 2 | 低 | ✅ 可选 |
| Markdown 博客 | P3 | 3 | 5 | 中 | 📋 v1.5+ |
| 访客统计 | P2 | 2 | 4 | 低 | 📋 v1.5+ |
| 多语言前台（zh/en） | P3 | 3 | 3 | 中 | 📋 v1.5+ |
| RSS 输出 | P3 | 2 | 2 | 低 | 📋 v2.x |
| 2FA / TOTP | P2 | 3 | 3 | 低 | 📋 v2.x |
| 主题市场（从 URL 加载社区主题） | P2/P3 | 3 | 5 | 低 | 🤔 v2.x |
| 多用户 / 团队 | — | — | — | — | ❌ 不做 |
| 评论 / 论坛 | — | — | — | — | ❌ 不做 |

**下一个 Sprint 应该做的**（v1.1.x → v1.2）：
1. 修剪首页信息密度（去掉 4 个进度条 → 换成一段「关于我」/ 精选作品位）
2. 覆盖后台剩余硬编码字段（页脚版权、SEO meta、社交链接图标选择）
3. 主题预设（先做 3 套：默认粉、极夜黑、纸白）
4. 后端核心接口 e2e 测试

---

## 6. 产品原则 (Design Principles)

按重要性排序，冲突时上面的赢：

1. **一个人用 > 多个人用**
   —— 不做多租户、不做权限层级、不做「团队工作区」。个人主页就是个人的。

2. **默认值可用 > 配置项完备**
   —— 装完立刻是能看的主页，而不是「请先配置 20 个字段」。占位符也要能跑，但要显眼提示这是占位。

3. **删代码 > 加代码**
   —— 想加一个功能前，先问「能不能删掉一个来替代」。首页现在四个进度条 + 打字机就是反面教材。

4. **构建时确定 > 运行时判断**
   —— 例：图标子集在构建时生成，别在运行时 fetch CDN。字体、翻译、theme 都同理。

5. **失败要响 > 静默降级**
   —— JWT_SECRET 错了、ACME_EMAIL 填的是 `you@example.com`，就直接 fail-fast 拒绝启动。别让用户上线后才在证书申请阶段发现。

6. **能自托管 > 依赖外部服务**
   —— Google Fonts、Iconify CDN 这类国内不稳的依赖，一律 self-host。DiceBear 头像可选留 fallback。

7. **文档写给「明年的自己」**
   —— 每个 PR / commit 消息、每个 `docs/log/*.md` 都当作是给一年后忘光了细节的自己读的。

---

## 7. 已明确「不做」清单

写出来避免每季度都被人问一遍：

- ❌ **多用户系统** —— 项目定位是 personal homepage，加了就成了半吊子 CMS
- ❌ **富文本编辑器 / WYSIWYG** —— 后台字段是"设置项"，不是"文档"
- ❌ **前后台合并成一个 SPA** —— 前台要极简、后台要功能全，两套栈解耦更好维护
- ❌ **完全去后端的静态版本** —— 那是 Simple-Homepage 的领地，我们的 delta 就是有后端
- ❌ **NoSQL 存储** —— 配置量小、schema 稳定，SQLite / MariaDB 足够
- ❌ **移动端 App** —— 后台已经响应式，够用

---

## 8. 参与贡献前请先读

- 想提 issue：先看这份文档的 §3（是不是目标用户）+ §7（是不是不做的）
- 想提 feature PR：先在 issue 讨论 §5 的优先级，避免写完被拒
- 想提 bugfix PR：直接来，不需要讨论
- 想加新组件：读 [docs/dev-guide.md](./docs/dev-guide.md) + §4 Journey C

—— 感谢每一位 star / fork / issue 的朋友。
