# 前台功能开关。构建时读取。
# 复制此文件为 .env 并按需修改。

# 站点线上域名（不带尾斜杠），用于生成 canonical / og:url / sitemap 绝对链接。
# 未配置时会退回到相对路径，SEO 收益会打折。
VITE_SITE_URL=https://example.com

# PWA（Service Worker + Manifest + 离线缓存）
# 默认关：多数个人主页并不需要 PWA，反而增加维护复杂度
# VITE_PWA_ENABLED=true

# Ambient 装饰层：磨砂玻璃 orbs 背景 + grain 噪点
# 默认关：视觉更简洁，性能更好
# VITE_AMBIENT_ENABLED=true
