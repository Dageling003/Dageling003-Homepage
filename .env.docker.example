# ====================================
# homepage — Docker 部署环境变量
# 复制为 .env.docker 后修改
# ====================================

# 域名或 IP（必填）
# - 域名（如 myhomepage.example.com）→ 自动申请 Let's Encrypt HTTPS 证书
# - 内网 IP（如 192.168.1.100）→ 仅 HTTP
DOMAIN=your-domain-or-ip

# JWT 密钥（至少 20 位，启动时强制校验）
# 生成命令：openssl rand -base64 32
JWT_SECRET=replace-with-a-strong-random-secret

# 默认管理员密码（至少 12 位，首次初始化时创建 admin 账号）
# 生成命令：openssl rand -base64 24
DEFAULT_ADMIN_PASSWORD=replace-with-a-strong-password-at-least-12-chars

# MariaDB 数据库配置
DB_ROOT_PASSWORD=your-db-root-password
DB_USERNAME=homepage
DB_PASSWORD=your-db-password
DB_DATABASE=homepage
